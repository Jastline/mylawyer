name: mylawyer
description: A minimal lawyer app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.19.0'  # Добавьте минимальную версию Flutter

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0  # Обновите до последней стабильной версии
  path: ^1.8.3    # Обновите до последней версии

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0  # Добавьте актуальную версию lints

flutter:
  uses-material-design: true
